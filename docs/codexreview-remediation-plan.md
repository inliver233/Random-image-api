# Codex Review Remediation Plan

> Branch: `dev`
> Baseline: `4c07a5482dda2ff7810a61ae0054ad195c8ec852`
> Audit source: root `codexreview.md` (ignored by `/*.md`, read directly)
> Rule: an item is marked `completed` only after code/history revalidation, a regression test, relevant integration checks, independent review, and a recorded commit.
> **2026-07-26 recalibration:** an independent 35-agent read-only re-audit at tip `08862c0` recorded the authoritative per-item status, gate numbers, public-deployment probes, god-file ledger and six newly discovered gaps in `docs/audit-2026-07-26-baseline.md`. Where this matrix and that document disagree, the baseline document wins. Public deployment now serves `08862c0` (OPS-ROLL complete); B2 was reclassified from environment-blocked to a code defect; completed B8 introduced a Uvicorn access-log regression (GAP-1).

## Working rules

- Never switch to, commit to, rewrite, or push `main`.
- Preserve pre-existing untracked `.workflow/**`, `backend/nul`, `nul`, `sh.exe.stackdump`, `新建 文本文档.txt`, `review出现偏差.txt`, and `任务全面详细要求.txt`.
- Stop rather than overwrite a pre-existing tracked modification.
- Stage only the files for the current independently verified change.
- PostgreSQL, Redis, Compose, Worker runtime, deployment, and benchmark items remain pending until their real environment evidence is captured; documentation or offline SQL is not sufficient.

## Status legend

- `pending`: audit claim mapped but not independently reproduced yet.
- `in-progress`: current code/history/tests have been inspected and remediation work has started.
- `blocked`: implementation or validation requires unavailable external state; exact blocker and runnable commands must be recorded.
- `completed`: fix and required evidence are present in the listed commit.

## Dependency order

1. **P0 release invariants:** B1, B8, B4, H12, B5, B6, B7, pximg redirect boundary checks, backend/frontend gates, H20.
2. **PostgreSQL data plane:** B2, B3, H5, H6, M4, M5, H4, M6, H7, M8, M9.
3. **Engine correctness/performance:** H1, H2, H3, M18, M19, then durable outbox/sequence/recovery and canary evidence.
4. **Edge/SLA:** H8-H11, H13-H16, M10-M11, M20.
5. **Admin/modularity/quality:** H17-H19, M1-M3, M7, M12-M17, M21.

### Current user-perceived performance subphase

1. **P-PERF-1 / H11 — completed slice (`fe7f3cf`):** cold GET streams after a validated bounded header; HEAD is metadata-only; small-object persistence is bounded and backpressured; isolate-local singleflight, cold-fill limits/deadlines, R2 error tri-state, validation markers, image size/MIME/magic/dimension checks and guarded prewarm are covered by runtime tests. H11 remains `in-progress` until Miniflare/staging, Range behavior and production TTFB evidence are recorded.
2. **P-PERF-2 / H8 — completed slice (`52ebc9b`):** API direct and residential-proxy image streams use independent data-plane transports, clients, proxy pools, limits and shutdown hooks. Control-plane Engine/CF/Admin/R2-prewarm/hydrate/OAuth/probe users remain on the original control state. H8 remains `in-progress` until the byte plane is moved off the BFF or production load/FD evidence closes the interim relay capacity.
3. **P-PERF-3 / feed-wtf — completed slice (`f90b29a`):** `/feed` performs one bounded Python batch call instead of up to 32 serial picks, with an explicit batch-level random/quality sampling contract, soft-dedup partial fill and Engine/Python uniqueness. `/wtf` shares one in-flight feed request, fences reset generations before queue consumption, and no longer consumes replacement picks after invalid metadata or primary+fallback image failure. Gateway/content failure classification remains H10 rather than being silently claimed here.
4. **P-PERF-4 / H4 — completed SQLite/default slice (`70f439c`, `c1ff93a`):** the default strict-safe Python fallback now has a `(status, x_restrict, random_key)` index, migration/metadata parity and bound/wrap EXPLAIN assertions with no temporary order sort. H4 remains `in-progress` until live PostgreSQL 16 plans and non-default `r18=2`/non-strict traffic evidence determine whether another index is justified.

Engine traffic remains 0, R2 remains optional, and B6/B7 remain honestly in progress.

## Audit item matrix

The root-cause text below is the audit hypothesis to revalidate, not completion evidence.

| ID | Status | Root cause to revalidate | Planned implementation | Primary files | Required tests/evidence | Depends on | Commit |
|---|---|---|---|---|---|---|---|
| B1 | completed | Production alias handling diverged between defaults and `is_prod`; known example credentials were accepted. | Centralized production detection and rejected the built-in and `.env.example` credential placeholders. | `backend/app/core/config.py`, config tests | Reproduced 6 failing cases before fix; Python 3.11 compileall; `17 passed`; independent agent review found no blocker. Full backend gate remains tracked by H20. | none | `9c95eef` |
| B2 | completed (live-PG evidence pending) | Alembic stripped `+asyncpg` to a bare URL, resolving the psycopg2 DBAPI that exists in no requirements file — a code defect, not an environment one. | Online migrations now run through SQLAlchemy's async engine (`run_sync`); bare `postgresql://`/`sqlite://` URLs normalize to the shipped async drivers; offline SQL rendering unchanged; asyncpg added to requirements-dev. | `backend/alembic/env.py`, requirements-dev.txt, `backend/tests/test_alembic_online_async_driver.py` | Regression test proves asyncpg (never psycopg2) resolution and a clean SQLite upgrade-to-head; all migration suites pass. Live PG16 `alembic upgrade head` remains blocked: Docker daemon unavailable on this host. | B1 | `3e784b7` |
| B3 | completed (live-PG evidence pending) | PG job candidate select lacked row locking; two transactions could claim the same job. | PG claim locks the candidate row with `FOR UPDATE SKIP LOCKED` in the claiming transaction; SQLite keeps the plain CTE. | `backend/app/jobs/claim.py`, `backend/tests/test_jobs_claim_postgres.py` | SQL-shape tests pass; SQLite concurrency suite passes; the 5-claimers/3-jobs live test is gated on `TEST_POSTGRES_URL` (no local PG — Docker unavailable). | B2 | `80af2b9` |
| B4 | completed | Hydrate checked enabled tokens only after excluding attempted ids, converting transient exhaustion to permanent failure; an exact prior defer could also be replaced by a default 60-second defer. | Separated global availability from per-run exclusions and propagated the previous `JobDeferError` unchanged. | hydrate handler and tests | Three audit regressions reproduced; no-token permanent case retained; forced 300-second backoff preserved; complete handler file `7 passed`; independent review passed. | none | `0ce6191` |
| B5 | completed | Conflict upsert mutated immutable import ownership; rollback keyed off that field, hydrate/import retries could outlive rollback, and rollback omitted Engine convergence. | `created_import_id` is immutable; explicit per-import memberships record verified ownership; a durable rollback tombstone stops import/hydrate work under row locks; rollback changes only `was_created` rows and publishes status/delete events, including a corrective status publish when rollback overlaps an in-flight import event. Historical memberships are fail-safe backfilled as unverified. | image upsert/mark, imports API, import/hydrate handlers, models/migration, Engine sync | Python 3.11 compileall; 60 related tests; clean SQLite upgrade/backfill/downgrade; duplicate ownership, pending rollback, hydrate membership, disable/delete events and overlap correction covered; independent review approved. Live PostgreSQL concurrency and durable Engine delivery remain B2/B6 evidence, not B5 completion criteria. | none | `9e472f4` |
| B6 | in-progress | Partial/event-only indexes could look ready, malformed or stale snapshots could replace the live state, and the protocol had no durable ordering/recovery. | Admin partial limits remain dry-run only. Formal push now reads authoritative count plus all keyset pages in one explicit SQLite/PG consistent transaction, sends complete/count/full canonical hash/revision/base-state manifest, and verifies the exact acknowledgement. Go rejects malformed/partial snapshots, keeps event-only and empty indexes not ready, atomically CAS-swaps only at the observed `state_version`, and separates verified snapshot identity from current event state. OpenAPI documents real snapshot/event/health/409 DTOs. Durable outbox, sequence/gap/replay, restart persistence, remaining producer convergence and high-performance chunked snapshots remain. | maintenance API, Engine sync/client/catalog, Go engine, contract/docs | Python 3.11 compileall; 70 related backend tests; actual SQLite same-count concurrent replacement stays on one read snapshot; Python/Go full-payload Unicode/NULL/float hash golden; Go test/vet/build and gofmt pass; direct partial/empty/event-only/invalid manifest/stale CAS/Admin legacy-health tests pass; independent review approved this subphase. `go test -race` is environment-blocked because local GCC lacks 64-bit cgo support. | H2/outbox and H1-H3 performance follow this protocol skeleton | partial: `3f09bec`, `8d6aba9` |
| B7 | in-progress | Runtime CF pools accepted weakly proven targets; deploy/register/delete had no durable external-effect identity, public health JSON was forgeable as proof, and image streaming auto-followed unvalidated redirects. | Strict verified `workers.dev` trust and bounded backend/img-worker redirects are in place. A durable `(account_id, worker_name)` deployment identity now exists with cross-kind uniqueness, pre-CF intent, operation-version fencing, conditional `intent→deployed/failed` transitions, redacted failure storage, and retryable transport failures. Successful rows deliberately stop at `deployed`: authenticated Worker challenge, encrypted secret/build reuse, atomic activation materialization, resumable register/delete and DNS/IP revalidation remain before `verified/complete`. | CF registry/admin/deployment persistence/proxy/OAuth/hydrate/image stream | Prior trust/redirect suites passed. Deployment foundation: Python 3.11 compileall; 31 CF/admin/migration tests plus 11 redaction tests; actual SQLite upgrade/downgrade and concurrent same-kind single-winner coverage; cross-kind request reaches zero CF calls; transport failure becomes redacted `failed` and can reserve operation version 2. Independent review blockers for fencing/redaction/transport wedge were addressed; full vertical slice still pending challenge. | none | partial: `f92381d`, `257c8d4`, `b9d07f4`, `0abbf4d` |
| B8 | completed | Redaction filter was attached to the root logger rather than the handlers that process propagated child/Uvicorn records. | Installed one idempotent filter on root and Uvicorn handlers. | logging config and tests | Reproduced real child/access leakage; Python 3.11 compileall; `11 passed`; child, access, error, idempotence and test-state isolation covered; independent review passed. | none | `00d76b2` |
| H1 | pending | Go pick scans every image while holding read lock. | Immutable snapshot pointer plus bitmap/posting intersections and candidate-only scoring. | `services/random-engine/**` | Golden filters; 10k/100k/621k/1M benchmarks; race test | B6 protocol | pending |
| H2 | pending | Every event rebuilds and sorts the entire index under write lock; sync is gated by URL rather than a distinct flag. | Add `RANDOM_ENGINE_SYNC_ENABLED`; asynchronous outbox consumer; incremental/COW batch updates and atomic swap. | backend Engine sync/config/deploy, Go engine | Import/hydrate non-blocking tests; event throughput and race benchmark | B6 | pending |
| H3 | pending | Snapshot pages accumulate into one Python list and one large JSON body. | Versioned begin/chunk/commit or efficient binary artifact with row count/hash. | Engine sync/client, Go snapshot transport | Interrupted chunk, hash/count mismatch, resume/atomicity, 621k memory report | B6 | pending |
| H4 | in-progress | The legacy filter index placed orientation/width/height between default `status,x_restrict` equality and `random_key`, forcing SQLite to build a temporary B-tree for the ring order. | Added the portable default-hot-path `(status, x_restrict, random_key)` index in ORM metadata and migration 0022. SQLite bound and wrap plans must select it without a temporary sort. Do not add `(status, random_key)` or a partial PG index until non-default traffic and live PG plans justify their write/storage cost. | image model/migration/random pick tests | Python 3.11 compileall; clean SQLite upgrade/downgrade; PostgreSQL offline DDL; 29 related tests; actual default/cooldown bound+wrap EXPLAIN; 100k-row comparison 5.328→0.009 ms mean. Live PG16 `EXPLAIN (ANALYZE, BUFFERS)` remains blocked with B2. | B2 for PG completion only | partial: `70f439c`, `c1ff93a` |
| H5 | completed | `driver_param_marker` returned `%s` for PostgreSQL, but asyncpg's paramstyle is numeric_dollar — worker heartbeat, admin summary and persisted totals reads all failed under asyncpg, totals silently zeroed. | All three call sites use `sa.text(:key)` named binds; the marker helper is deleted and its absence pinned by test; totals gained a round-trip test. | healthz, summary, random_request_persistence, images_upsert, tests | `f936aee` suites pass; the fossilized `%s` expectation test replaced. | B2 | `f936aee` |
| H6 | completed | Retry predicate unwrapped only `OperationalError`; asyncpg Deadlock/Serialization errors wrap as generic `DBAPIError` and never matched. | Predicate inspects driver SQLSTATE (40001/40P01/55P03) on wrapped and bare errors; hard failures still fail loud. | `backend/app/db/session.py`, `backend/tests/test_db_contention_retry.py` | Real asyncpg exception types wrapped via SQLAlchemy's own path; retry/no-retry wrapper behavior covered. Live contention test remains a PG-environment item. | B2 | `9a6ffab` |
| H7 | completed (live-PG evidence pending) | Legacy migration was partial, row-by-row, non-resumable, and did not preserve/validate relationships. | New `scripts/legacy/etl_sqlite_to_postgres.py`: metadata-derived columns/FK edges, preserved PKs + sequence reset, row-value keyset batches, asyncpg COPY, checkpoint/resume with crash-window repair, orphan preflight/null-out/drop with counts, non-empty-target guard, count+PK-checksum+orphan validation. | `scripts/legacy/etl_sqlite_to_postgres.py`, `backend/tests/test_legacy_etl.py` | 14 tests run the pipeline end-to-end on a fresh alembic SQLite target (copy, resume-exactly-once incl. composite-PK repair, orphan null-out/drop, wipe guard, drift detection); adversarial review findings fixed. Live PG e2e + real 62万 migration remain gated on `TEST_POSTGRES_URL`/Docker. | B2, M4, M5 | `9447c3e` |
| H8 | in-progress | Same-origin semantics still relay bytes through Python, and image streams previously shared both direct and residential-proxy capacity with OAuth/hydrate/Admin/Engine/R2-prewarm traffic. | The interim BFF relay now has explicit control/data planes: distinct transports/clients, distinct proxy pools, per-plane configurable direct/proxy connection limits, correct production proxy construction, request-scoped timeouts, lease-safe stream cleanup and full lifespan shutdown. `/random` image plus `/i`/legacy delivery use data state; feed/Engine/Admin/CF and jobs retain control state. Final same-origin Cloudflare route/service-binding byte offload remains. | delivery/http clients, API lifespan, deploy env, tests | Python 3.11 compileall; 31 client/stream/random unit tests and 20 controlled image/random integration tests pass; 13 related error/route tests pass; blocked-data synthetic test leaves control request mean 0.132 ms/p95 0.239 ms across 20 samples; independent review approved. Production concurrent stream/FD/TTFB and final byte offload remain. | H9-H11 | partial: `52ebc9b` |
| H9 | pending | Candidate resolver exists but production delivery uses one signed URL and records no online base outcome. | Iterate verified candidates within a global deadline and feed cooldown/circuit outcomes. | image edge/delivery | Multi-base fault-injection tests | B7, H10 | pending |
| H10 | pending | Worker/gate/storage errors are treated as image content failures. | Return structured failure classes; only trusted origin content failures mutate image health. | BFF delivery, img-worker | Fail-count invariants for Worker/R2/secret/rate-limit failures | H9 | pending |
| H11 | in-progress | Cold misses and HEAD buffered/downloaded complete objects; unrestricted tee branches amplified memory; storage failures/misses were conflated; no image size/content validation or cold-fill coordination existed. | Cold GET now returns after a bounded validated header and follows client backpressure without `clone()`/full buffering. HEAD uses Cache/R2/origin metadata only. Known small objects use one preallocated buffer after client completion, with validation markers, bounded Cache/R2 writes, isolate-local singleflight, four-slot concurrency, idle/total deadlines, strict length/MIME/magic/dimension/pixel/byte checks, R2 hit/miss/error classification and guarded prewarm cleanup. | `edge/img-worker/**` | Node runtime and pure tests: 76 pass; paused client does not drain origin; 50 same-key requests make one origin fetch; stalled slots recover; corrupt/oversize/mismatched Cache/R2/origin/prewarm cases fail closed; synthetic 2 MiB delayed-tail benchmark reduces GET first-byte 130.29→5.31 ms and HEAD handler 128.91→0.77 ms. Independent review approved. Miniflare/staging, Range and real-network benchmark remain. | H10 | partial: `b9d07f4`, `fe7f3cf` |
| H12 | completed | Proxy URI key omitted endpoint/database fields and invalidation missed it; pooled clients froze the first timeout and LRU eviction could close active requests. | URI cache uses database plus the complete connection fingerprint and unified invalidation. Pooled clients now require cancellation-safe leases, active entries cannot be evicted, released entries have idle/LRU cleanup, and stream/hydrate/probe/OAuth apply request-scoped timeouts. | HTTP client/proxy routing | URI/routing regression suite passed. Lease tests cover reuse, active overflow, idle eviction, timeout overrides, acquire cancellation and release cancellation. Python 3.11 compileall; 35 related tests passed; independent review found and verified both cancellation leak fixes. | none | `3072c3e`, `75b9cd3` |
| H13 | pending | Worker gate and Pixiv business statuses share codes; layered retries lack operation deadline and can repeat OAuth POST. | Structured gate codes, capped candidates/retries, one deadline, idempotency-aware retry policy. | API Worker, CF proxy, selector, hydrate/OAuth | Runtime status matrix, timeout budget, POST no-replay tests | B7 | pending |
| H14 | pending | R2 enqueue is synchronous serial work and 2xx hides partial failures. | Durable async queue/job with idempotent items, bounded concurrency, partial result/retry semantics. | prewarm, handlers, img-worker | Partial failure, retry, backpressure and non-blocking job tests | H11 | pending |
| H15 | pending | CF external effects and local pool persistence are non-atomic; backend lacks cross-kind identity enforcement. | Durable idempotent deployment state machine, intent/complete phases, drain-delete, server uniqueness and probe/challenge. | CF admin/deploy/persistence/models | Crash/retry state-machine tests; same-name rejection; probe-before-enable | B7 | pending |
| H16 | pending | Metrics/circuits/overlays are process-local and healthz is used as readiness. | Split `/livez` and `/readyz`; aggregate/scrape processes; publish one effective-config snapshot and instance identity. | health/metrics/deploy/status | API+worker+replica aggregation; dependency fault readiness tests | B2, H9 | pending |
| H17 | pending | SPA fallback treats `api-keys` as an API path. | Match only `api` or `api/`; generate deep-link coverage for every nav route. | admin UI fallback, frontend routes/tests | Dist-backed deep-link tests | frontend gate | pending |
| H18 | pending | Multi-key settings update creates and commits one session per key. | Batch upsert in one Unit of Work and invalidate only after commit. | settings API/runtime settings | Rollback-on-Nth-error and success atomicity tests | M1/UoW direction | pending |
| H19 | pending | Paginated pool refresh reinitializes dirty local state; falsy coercion changes weight zero to one. | One-time initialization/revisioned merge and explicit finite-number parsing. | ProxyPools page/API | Multi-page draft and weight=0 UI/API tests | frontend gate | pending |
| H20 | pending | CI omits dev, Go, live PG, image builds, Worker runtime, coverage; current gates are red. | Restore all existing suites, listen to dev, add Go/PG/coverage/image/runtime jobs incrementally. | `.github/workflows/**`, Dockerfiles/scripts | CI-equivalent local commands and workflow review | preceding P0 fixes | pending |
| M1 | pending | Ports expose `AsyncSession`/ORM and do not own persistence boundaries. | Business DTO/query ports plus explicit UnitOfWork; migrate call sites incrementally. | catalog/tag/random ports and consumers | Contract tests with alternate fake implementation | H18, PG foundation | pending |
| M2 | pending | `memory` is an alias for the same DB queue and labels hide the real dialect. | Rename to `DatabaseJobQueue`, report dialect honestly, or implement a real external backend via outbox. | jobs queue/contracts/status | Factory/backend honesty and integration tests | B3 | pending |
| M3 | pending | Import jobs reference a local relative payload file. | BlobStore port with content hash/object key; local and object-storage implementations. | import API/jobs/storage | API/worker separate-filesystem test, resume/dedup | M1 | pending |
| M4 | completed | ORM partial unique index lacked `postgresql_where` (compiled PG metadata was an unconditional global unique) and Alembic ignored metadata. | Identical predicate on both dialects; `target_metadata=Base.metadata` wired in env.py. | jobs model, Alembic env, `test_jobs_metadata_parity.py` | Both dialects' DDL contains the predicate; offline PG SQL still correct. | B2 | `b8aaf1d` |
| M5 | completed | Migration 0019 created the unique partial index without deduping existing active duplicates, aborting upgrades. | Deterministic preflight cancels all but the oldest active duplicate per key before index creation. | migration 0019, `test_jobs_metadata_parity.py` | Duplicate-upgrade fixture passes on SQLite; the same SQL is dialect-neutral. | B2, M4 | `b8aaf1d` |
| M6 | completed (live-PG plans pending) | PG search fallback used case-sensitive unindexed `%LIKE%`. | Fallback is ILIKE on both dialects; migration 0023 adds pg_trgm GIN indexes (PG-only, SQLite no-op). | tags/authors list, migration 0023, `test_search_case_insensitive.py` | Mixed-case round trip, PG ILIKE compilation, SQLite head-upgrade no-op all pass. Query plans at scale need live PG. | B2 | `d1757e4` |
| M7 | completed | Admin image list pre-aggregated all image_tags per page request. | TAGS-1 pattern: page ids first, COUNT only page ids; missing-tags becomes NOT EXISTS. | `backend/app/db/images_admin_list.py` | Catalog + admin filter suites pass unchanged. | none | `ccb37b9` |
| M8 | completed | Production permitted default SQLite by omission. | Prod refuses any SQLite URL unless `ALLOW_PROD_SQLITE=true`; placeholder rejection was B1. | config, tests, deploy/.env.example | Prod matrix incl. override passes. **Ops impact: current SQLite production deployment must set the override before rolling this commit.** | B1 | `25a53c8` |
| M9 | completed | SQLite defaults allowed 50 concurrent writers against the single-writer catalog. | `apply_write_budget()`: SQLite unset → cap 8, explicit 0 → uncapped, PostgreSQL uncapped by default. | worker, deploy/.env.example, `test_worker_write_budget.py` | 6 unit tests + worker smoke pass. | M8 | `ebb5fa4` |
| M10 | pending | Each API replica overwrites a shared random-total value. | Use metrics as source or atomic deltas/per-instance shards. | totals persistence/status | Two-instance monotonicity test | B2/Redis decision | pending |
| M11 | pending | Key cooldown/circuit/emergency states are isolated per process. | Share critical revisions/state or explicitly surface per-instance degraded semantics. | Engine/CF/Redis runtime state | Multi-replica consistency/failover tests | H16 | pending |
| M12 | pending | Dashboard merges image/API pool status and renders partial failure as healthy. | Independent domain status with unknown/loading/error states and no false green. | Dashboard page/tests | Partial CF outage UI tests | H16 | pending |
| M13 | pending | Proxy attach defaults to pool id 1 and partial import errors are hidden. | No default until enabled pools load; validate selection; expose structured partial errors. | Proxies page/tests | Non-1 pool and partial-success tests | frontend gate | pending |
| M14 | pending | All routes are statically bundled and hidden panels continue polling. | Route-level `React.lazy`; query `enabled` by active panel; bundle budget. | App/routes/pages/query hooks | Route/deep-link tests, hidden polling tests, chunk report | H17 | pending |
| M15 | pending | Several Admin pages combine unrelated state, requests and views in one component. | Split by meaningful feature boundaries without broad cosmetic churn. | Admin pages/features | Feature-level tests and unchanged flows | H17-H19, M12-M14 | pending |
| M16 | pending | Hydrate/import/app/Go/HTML builders remain giant units. | Split domain workflows and staticize HTML/JS assets incrementally. | hydrate/import/main/Go/wtf/status/docs | Existing regression suites plus new unit boundaries | P0-P3 stabilization | pending |
| M17 | pending | Broad catches and silent passes hide failed invariants. | Narrow exception boundaries; structured degraded/retry states; fail loud for consistency. | backend hotspots | Fault-injection tests and static trend report | per-module fixes | pending |
| M18 | pending | Engine OpenAPI types/fields differ from actual Python/Go DTOs. | Generate/validate one truthful schema and golden vectors. | Engine contract/client/Go DTO | Schema validation and round-trip tests | B6, H3 | pending |
| M19 | pending | Python/Go seeded selection and NULL/date/filter boundaries diverge. | Define routing/seed contract and share golden vectors/PRNG where required. | Python pick/Engine filters/tests | Cross-process vectors including seed, NULL, dates, quality | H1, M18 | pending |
| M20 | pending | Runtime settings store CF/Image Edge secrets as plaintext JSON. | Secret reference or field-level encryption with rotation/versioning. | runtime settings/CF/image edge/crypto | DB-at-rest assertions, rotation and migration tests | B7, H15 | pending |
| M21 | pending | README/contracts/cutover docs and tracked-source assumptions have drifted. | Update only after implementations stabilize; keep benchmark/acceptance evidence under tracked docs. | README/contracts/scripts/docs | Link/checklist/schema consistency check | all relevant items | pending |

## Cross-cutting requirements not represented by one audit ID

| Requirement | Status | Mapping / evidence plan |
|---|---|---|
| Strict pximg hostname boundary and per-hop redirect validation | in-progress | Backend and img-worker hostname/HTTPS/authority policies, bounded manual redirects, cross-boundary rejection and hop limits are completed in `e905d7b`, `257c8d4`, and `b9d07f4`. DNS/private-address/rebinding controls and a runtime Worker integration test remain under B7/H11/H13. |
| Rollback/delete/status/fail-cooldown Engine synchronization | in-progress | B5 now publishes rollback disable/delete events and corrects an overlapping stale import publish. B6 must still enumerate remaining status/fail-cooldown producers and prove durable ordered outbox convergence. |
| Separate Engine sync and traffic flags | pending | Covered by H2; defaults must keep both false until complete snapshot readiness. |
| Production canary 5→25→50→100 with rollback | blocked | Requires deployed production-like environment after B6/H1-H3/M18-M19; save commands and results under `docs/benchmarks/`. |
| Main/dev and pre/post performance comparison | in-progress | P-PERF-1/2/3 synthetic comparisons are tracked under `docs/benchmarks/`; the reproducible 5k/100k/621k/1M matrix is still required. |

## Validation log

| Date | Scope | Command/evidence | Result |
|---|---|---|---|
| 2026-07-13 | Initial repository guard | `git branch --show-current`; `git rev-parse HEAD`; `git status --short --branch` | On `dev` at baseline; only the listed pre-existing untracked files were present. |
| 2026-07-13 | B1 failing regression | `py -3.11 -m pytest -q backend/tests/test_config.py` before implementation | `production` accepted default credentials and all four known placeholders; 6 failures reproduced. |
| 2026-07-13 | B1 fix | `py -3.11 -m compileall -q backend/app/core/config.py backend/tests/test_config.py`; `py -3.11 -m pytest -q backend/tests/test_config.py` | PASS; 17 tests. Independent read-only review: no blocking issue. |
| 2026-07-13 | B8 failing regression | `py -3.11 -m pytest -q backend/tests/test_log_redaction.py` before implementation | Configured child and Uvicorn access records leaked their secrets; 2 failures reproduced. |
| 2026-07-13 | B8 fix | `py -3.11 -m compileall -q backend/app/core/logging.py backend/tests/test_log_redaction.py`; `py -3.11 -m pytest -q backend/tests/test_log_redaction.py` | PASS; 11 tests. Independent read-only review approved handler coverage, idempotence, and test isolation. |
| 2026-07-13 | B4 reproduction | Three focused hydrate executor tests on the baseline | Rate-limit, refresh network error, and proxy-required all transitioned to DLQ; the combined command exceeded the local timeout while the agent's isolated reproduction recorded all three failures. |
| 2026-07-13 | B4 fix | `py -3.11 -m compileall -q backend/app/jobs/handlers/hydrate_metadata.py backend/tests/test_job_handler_hydrate_metadata.py`; `py -3.11 -m pytest -q backend/tests/test_job_handler_hydrate_metadata.py` | PASS; 7 tests. A forced 300-second defer proves exact `run_after` propagation. Independent review approved completion. |
| 2026-07-13 | H12 URI cache | Focused pytest; `pytest.main(['-q','backend/tests/test_proxy_pool_routing.py'])` followed by `os._exit`; `py -3.11 -m pytest -q backend/tests/test_proxy_client_pool.py` | PASS; 2 focused, 8 routing, 4 proxy-client tests. Ordinary routing-file pytest completes tests but the process retains a pre-existing app thread; wrapper records the test result and exits. H12 remains in progress. |
| 2026-07-13 | H12 client lease/timeout completion | Baseline lease tests reproduced 5 failures because the pool had no lease API, could evict active clients, and froze construction-time timeouts. Python 3.11 compileall; `pytest.main` for proxy client/stream/routing/probe/OAuth/hydrate tests; two independent cancellation reviews | PASS; 35 related tests. Active leases may temporarily exceed the cap instead of being closed; cleanup is idle/LRU and cancellation-safe; all affected requests set their own timeout. The internal `acquire_proxy_client` API now returns a lease and all repository callers were migrated. |
| 2026-07-13 | B6 partial-snapshot guard | Python 3.11 compileall; `pytest.main` for Engine events, warm startup and Admin Engine tests | PASS; 18 tests. Null/zero/invalid limits fail closed, positive limits are dry-run only, formal no-limit push is exactly once, and core rejects partial formal push before DB/HTTP. B6 remains in progress. |
| 2026-07-13 | Strict pximg hostname | Python 3.11 compileall; `pytest.main` for `test_pixiv_urls.py` and `test_image_edge.py` | PASS; 34 tests. Deceptive prefix/suffix domains are rejected; independent review approved. Redirect hardening remains. |
| 2026-07-13 | B7 immediate CF trust closure | Python 3.11 compileall; focused registry/overlay/probe tests; focused Admin/deploy tests | PASS; 18 core CF and 25 Admin/deploy tests. Runtime bases now require both requested and verified state and strict `workers.dev` HTTPS form. Legacy runtime-only bases fail closed on upgrade and must be redeployed or configured as trusted environment bases. B7 remains in progress pending durable identity/challenge and DNS/redirect controls. |
| 2026-07-13 | Backend image redirect hardening | Baseline `test_http_stream.py` reproduced 3 failures: auto-follow enabled, link-local redirect requested repeatedly, and HTTP initial URL accepted. Python 3.11 compileall; focused stream/Pixiv tests; import/image-delivery integration subset; read-only independent review | PASS; 25 focused and 47 related tests. Manual redirects are capped at 3, intermediate responses close, rejected targets are never requested, and ingestion now rejects HTTP/userinfo/non-default ports. Repository SQLite samples contain zero `http://` image URLs. DNS/rebinding controls remain. |
| 2026-07-13 | img-worker redirect hardening | Baseline inspection confirmed both origin and mirror fetches used `redirect: "follow"`; pure redirect tests failed before helper exports existed. `node --check src/index.js`; `node --check src/pure.js`; `node --test test/pure.test.mjs` | PASS; 53 tests. Origin redirects stay within strict pximg boundaries, mirror/worker redirects stay on the selected host, at most three hops are followed, and intermediate bodies are canceled. Miniflare/runtime integration and DNS/rebinding controls remain. |
| 2026-07-13 | B5 import ownership and rollback | Baseline duplicate-import regression reproduced reassignment of `created_import_id`. Python 3.11 compileall; controlled `pytest.main` for upsert/catalog/import/hydrate/Admin/Engine/migration/OpenAPI tests; clean SQLite Alembic upgrade, legacy backfill and downgrade; independent review | PASS; 60 related tests. Creator ownership remains immutable, only verified `was_created` memberships roll back, pending and queued import/hydrate work observes the durable tombstone, and disable/delete plus overlap-correction events converge the current Engine path. Historical rows are deliberately backfilled unverified, so operators must audit/repair legacy ownership before rolling those imports back. Durable delivery/sequence remains B6 and Engine traffic stays disabled. |
| 2026-07-13 | B6 snapshot safety protocol | Python 3.11 compileall and controlled related pytest; actual SQLite WAL same-count replacement during paged build; Python/Go canonical hash golden; `gofmt -d`; `go test ./...`; `go vet ./...`; `go build ./...`; OpenAPI parse/assertions; two independent review rounds | PASS; 70 related backend tests and full Go unit suite. Formal snapshots require verified count/full payload hash/revision and a `state_version` CAS; malformed, partial, event-only, empty and stale cases fail closed without false readiness. `go test -race ./...` remains unverified on this host: `cc1.exe: unimplemented: 64-bit mode not compiled in`. B6 remains in progress for durable outbox/sequence/gap/replay, restart recovery, producer inventory and chunk/performance work; Engine traffic remains 0. |
| 2026-07-13 | B7 durable deployment identity foundation | Python 3.11 compileall; controlled CF Admin/deploy/store/migration/offline-PG tests; standalone redaction suite; repeated independent review | PASS; 31 CF-related tests and 11 redaction tests. DB uniqueness prevents api/image overwriting the same account/name before Cloudflare is called; operation-version CAS prevents stale transitions; unexpected transport failures persist a redacted retryable `failed` state. Success remains honestly `deployed`, not verified/complete. Authenticated challenge, durable encrypted secret/build resume, transactional pool activation and delete/register recovery remain. |
| 2026-07-13 | P-PERF-1 / H11 img-worker cold streaming | Baseline code at `7dbb127` plus controlled 2 MiB/120 ms delayed-tail benchmark; `node --check` for `index.js`, `image_stream.js`, `pure.js`; `npm test`; static no-`clone()`/no-image-path-`arrayBuffer()` check; independent adversarial review | PASS; 76 tests. GET first byte no longer waits for the tail, paused clients do not cause background drain, 50 same-key cold misses produce one origin fetch, stalled clients release four bounded slots, HEAD reads no image body, and corrupt/oversize/length-mismatched Cache/R2/origin/prewarm objects fail closed with deterministic cleanup. Synthetic mean GET first-byte improved 130.29→5.31 ms and HEAD handler 128.91→0.77 ms; full transfer stayed tail-bound at 130.30→127.07 ms. Miniflare/staging/Range/real-network evidence remains, so H11 is in progress rather than completed. |
| 2026-07-13 | P-PERF-2 / H8 BFF pool isolation | Python 3.11 compileall; 31 focused proxy-client/http-stream/random-delivery tests; controlled `pytest.main` plus `os._exit` for 20 `/random`/`/i`/legacy/integration tests because the ordinary process retains a known app thread; 13 related error/origin routing tests; 20-sample blocked-data/control synthetic benchmark; independent review | PASS. Direct control/data transports and clients are distinct (100/40 vs 160/80); residential control/data clients and URI pools are distinct (40/16 vs 16/8, 32 URIs each). Production proxy delivery does not inject the direct transport. A blocked data request left control requests at mean 0.132 ms, p95 0.239 ms, max 0.339 ms. Lifespan closes both state clients and both lazy proxy/singleton planes. The full `test_error_codes.py` is not claimed green: its pre-existing no-token test still expects a defer although completed B4 intentionally treats zero enabled tokens as permanent; H20 must reconcile that test drift. |
| 2026-07-13 | P-PERF-3 / feed-wtf batching | Baseline `c0c741d`; Python 3.11 compileall; controlled focused/related pytest; generated WTF JavaScript `node --check`; executable Node stale-generation race harness; 10-run SQLite 10k-row/limit-32 comparison; two-round independent review | PASS; 42 related tests. `/feed` replaces 32 sequential Python pick calls with one bounded batch call (a second only for partial soft-dedup fallback), keeps Engine partial rows unique, and defines truthful batch-level random/quality sampling. `/wtf` old generations cannot shift new queue rows, and invalid metadata or failed primary+local image delivery cannot consume replacement picks. Synthetic fallback selection improved mean 106.559→3.134 ms (34.0×) and measured SQL statements 320→10 across ten runs. H10 structured infrastructure/content classification remains pending. |
| 2026-07-26 | Gate-zero closure | B8 access-log regression fixed (`e35a382`, 12 redaction tests incl. real AccessFormatter); frontend lint/typecheck restored (`0c6b366`); 13 drifted vitest tests reconciled (`cfa45c8`, full suite 73/73); 5 drifted backend tests reconciled (`71f1751`); CI listens to dev + Go job with `-race` (`7f6e8cd`); ordering-dependent redaction failures root-caused to alembic `fileConfig(disable_existing_loggers=True)` and fixed (`042ac4f`). | Backend full suite 813 passed / 1 skipped (live-PG gate); frontend lint 0 errors / typecheck 0 errors / vitest 73 passed / build pass; Go gofmt/vet/test/build pass locally. |
| 2026-07-26 | PG data-plane debt (B2, B3, H5, H6, M4-M8) | Commits `3e784b7`, `80af2b9`, `f936aee`, `9a6ffab`, `b8aaf1d`, `d1757e4`, `ccb37b9`, `25a53c8`; per-item evidence in the matrix above. | All targeted suites green; full backend rerun tracked below. Live PG16 evidence (migration, concurrent claim, contention, trgm plans) remains blocked: Docker daemon unavailable (`docker info` fails); tests are gated on `TEST_POSTGRES_URL`, and exact commands are recorded in each test's docstring. |
| 2026-07-26 | Full recalibration audit | 35 read-only agents + adversarial cross-check + local gates (`py -3.11 -m pytest backend/tests -q`: 791 passed / 7 failed; frontend lint 3 errors / typecheck 5 errors / vitest 13 failed 60 passed / build pass; Go gofmt/vet/test/build green, `-race` blocked by local GCC) + public probes (`/version`=08862c0, `/random` 200 `x-image-edge: stream`) | Authoritative per-item status recorded in `docs/audit-2026-07-26-baseline.md`: 7 items truly resolved / 10 partial / 32 untouched; B2 reclassified as a code defect (no sync PG driver exists in any requirements file); B8 fix regressed Uvicorn access logs (GAP-1); five backend test failures are deliberate semantic drift, two are ordering-dependent redaction tests; six new gap classes (GAP-1..GAP-6) added to scope. |
| 2026-07-13 | P-PERF-4 / H4 default Python pick index | Baseline `2aa544d`; exact default/cooldown SQLite EXPLAIN before/after; Python 3.11 compileall; clean SQLite Alembic upgrade/downgrade; PostgreSQL offline SQL generation; 29 related tests; 100k-row/200-sample forced-index benchmark; independent review | PASS for SQLite/default path. The legacy index used only `status,x_restrict` then `USE TEMP B-TREE FOR ORDER BY`; the new index applies `status,x_restrict,random_key` directly for both bound and wrap queries. Mean local lookup improved 5.328→0.009 ms, p95 7.016→0.013 ms. No random result semantics changed. H4 remains in progress because live PostgreSQL and non-default R18 plan evidence are unavailable. |

## Commit log

| Commit | Audit IDs | Verification |
|---|---|---|
| `6a6221f` | Tracking foundation | Added the audit matrix, dependencies, validation log, and tracked benchmark path exceptions. |
| `9c95eef` | B1 | Unified production aliases and rejected insecure built-in/example credentials; focused Python 3.11 tests pass. |
| `00d76b2` | B8 | Installed handler-level redaction across application and Uvicorn log paths with isolated integration tests. |
| `0ce6191` | B4 | Preserved recoverable hydrate deferrals and exact upstream/token backoff deadlines. |
| `3072c3e` | H12 (partial) | Corrected Proxy URI cache identity and invalidation; client lease/timeout work remains. |
| `75b9cd3` | H12 | Added cancellation-safe pooled-client leases, active-request protection, idle/LRU cleanup and request-scoped timeouts across stream/hydrate/probe/OAuth paths. |
| `3f09bec` | B6 (partial) | Sealed the known Admin partial-snapshot replacement path; protocol ordering/recovery remains. |
| `e905d7b` | P0 hostname boundary | Enforced strict pximg domain boundaries in import parsing and Image Edge signing. |
| `f92381d` | B7 (partial) | Closed arbitrary runtime CF pool enrollment and made legacy unverifiable overlays fail closed; durable deployment identity/challenge and DNS/redirect controls remain. |
| `257c8d4` | B7 / redirect boundary (partial) | Replaced backend image auto-follow with bounded per-hop validation and aligned Pixiv ingestion authority rules; DNS/rebinding and img-worker redirects remain. |
| `b9d07f4` | B7 / H11 redirect boundary (partial) | Replaced img-worker automatic redirects with bounded per-hop validation for origin and fallback mirrors; runtime integration and DNS/rebinding remain. |
| `9e472f4` | B5 | Preserved immutable import ownership, added verified import-image memberships and rollback tombstones, stopped rolled-back import/hydrate work, and synchronized disable/delete changes to the current Engine event path. |
| `8d6aba9` | B6 (partial) | Added manifest-verified complete snapshots, consistent DB reads, full cross-language payload hashes, Go readiness/CAS fencing, strict acknowledgements and truthful snapshot/current-state contracts. |
| `0abbf4d` | B7 / H15 (partial) | Added durable Cloudflare Worker identity/intents, cross-kind uniqueness, operation fencing, redacted failures and retryable transport-failure state without claiming challenge verification. |
| `fe7f3cf` | H11 / P-PERF-1 (partial audit item, completed performance slice) | Replaced full cold buffering and unsafe tee clones with validated backpressured streaming, metadata-only HEAD, bounded single-buffer persistence, isolate singleflight/deadlines, R2 tri-state/markers and guarded prewarm. |
| `52ebc9b` | H8 / P-PERF-2 (partial audit item, completed pool-isolation slice) | Split direct and proxy-bound image streams from the control-plane HTTP capacity, preserved lease/timeout/redirect semantics, added independent lifecycle/configuration and aligned legacy H0 tests with same-origin streaming. |
| `f90b29a` | P-PERF-3 / feed-wtf (completed performance slice) | Replaced serial fallback picks with bounded batch selection, preserved strict/soft recent-dedup and Engine top-up uniqueness, fenced stale waterfall consumers, and stopped invalid/failed image items from amplifying picks. |
| `70f439c` | H4 / P-PERF-4 (partial audit item, completed SQLite/default slice) | Added the default `(status, x_restrict, random_key)` ring-pick index with ORM/migration parity, SQLite EXPLAIN and upgrade/downgrade coverage. |
| `c1ff93a` | H4 / P-PERF-4 test evidence | Extended the ordered-index assertion to the wrap-around query as well as the initial `random_key >= r` leg. |
| `006ecd3` | 2026-07-26 baseline | Recorded the recalibration audit as `docs/audit-2026-07-26-baseline.md` (authoritative status reference). |
| `e35a382` | GAP-1 (B8 regression) | Element-wise redaction of uvicorn access args; access lines stay visible and redacted. |
| `0c6b366` | frontend gate | Production type error (RecommendationPage DEFAULTS), lint errors, mock tuple typing. |
| `cfa45c8` | frontend gate / M12 evidence | Reconciled 13 drifted admin-page tests; removed observability signals recorded under M12 in the baseline doc. |
| `7f6e8cd` | H20 (partial) | CI listens to dev pushes; Go job with gofmt/vet/test -race/build. Coverage/image/PG jobs remain. |
| `71f1751` | backend gate | Reconciled 5 drifted tests (no-token permanent, 409 token gate, OpenAPI copy, FastAPI route nesting). |
| `3e784b7` | B2 | Async online alembic migrations on shipped drivers; regression test pins asyncpg resolution. |
| `80af2b9` | B3 | PG claim uses FOR UPDATE SKIP LOCKED; live concurrency test gated on TEST_POSTGRES_URL. |
| `f936aee` | H5 | Replaced %s driver markers with sa.text named binds; deleted the marker helper. |
| `9a6ffab` | H6 | Retry predicate matches SQLSTATE 40001/40P01/55P03 through DBAPIError wrapping. |
| `042ac4f` | backend gate | alembic fileConfig no longer disables app loggers; redaction tests restore disabled state. |
| `b8aaf1d` | M4, M5 | Cross-dialect partial index predicate; 0019 dedupe preflight; metadata wired to alembic. |
| `d1757e4` | M6 | ILIKE fallback on both dialects; pg_trgm GIN indexes migration (PG-only). |
| `ccb37b9` | M7 | Admin images page-first tag counting (TAGS-1 pattern). |
| `25a53c8` | M8 | Prod refuses SQLite without ALLOW_PROD_SQLITE=true (ops impact documented). |
