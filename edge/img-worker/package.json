{
  "name": "random-image-edge",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker image edge for Random-image-api (signed pximg reverse proxy + Cache API)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
