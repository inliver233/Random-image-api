{
  "name": "random-image-api-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker API egress pool for Random-image-api (Pixiv OAuth/App API reverse proxy)",
  "type": "module",
  "scripts": {
    "test": "node --test test/pure.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
